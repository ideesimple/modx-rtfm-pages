a:5:{s:2:"id";s:8:"18678252";s:4:"info";a:4:{s:6:"Title:";s:13:"OnDocFormSave";s:7:"Author:";s:15:"Shaun McCormick";s:16:"Last Changed by:";s:17:"Everett Griffiths";s:12:"Wiki Markup:";s:28:"[revolution20:OnDocFormSave]";}s:6:"parent";s:8:"18678228";s:6:"source";s:1227:"\\

h2. Event: OnDocFormSave

Fires after a Resource is saved in the manager via the editing form.

Service: 1 - Parser Service Events
Group: Documents

h2. Event Parameters

||Name||Description||
|mode|Either &#39;new&#39; or &#39;upd&#39;, depending on the circumstances.|
|resource|A reference to the modResource object.|
|id|The ID of the Resource (even for new resources)|

h2. Examples

To do something with the page ID (e.g. to update a related custom table), you can read this out of the *$resource* object (even if you are creating a new resource):

{code}
// Log all available properties of the $resource
$modx-&gt;log(MODX_LOG_LEVEL_ERROR, print_r($resource-&gt;toArray(),true) );
// Get the page id
$page_id = $resource-&gt;get(&#39;id&#39;);
// or simply
$page_id = $id;

if ($mode == &#39;new&#39;) {
    // resource created
}
else {
   // existing resource was updated
}
{code}

Anything you return from this event will be written to the logs, e.g.

{code}
return &quot;Help I&#39;m a bug!&quot;;
{code}

Will result in a log message like the following:

{code}
 [2012-06-22 13:00:28] (ERROR @ /connectors/resource/index.php) [OnDocFormSave]Help I&#39;m a bug!
{code}

h2. See Also

* [System Events]
* [Plugins]";s:8:"versions";a:5:{i:0;a:3:{s:2:"id";s:8:"18678252";s:3:"ver";s:1:"5";s:4:"code";s:1227:"\\

h2. Event: OnDocFormSave

Fires after a Resource is saved in the manager via the editing form.

Service: 1 - Parser Service Events
Group: Documents

h2. Event Parameters

||Name||Description||
|mode|Either &#39;new&#39; or &#39;upd&#39;, depending on the circumstances.|
|resource|A reference to the modResource object.|
|id|The ID of the Resource (even for new resources)|

h2. Examples

To do something with the page ID (e.g. to update a related custom table), you can read this out of the *$resource* object (even if you are creating a new resource):

{code}
// Log all available properties of the $resource
$modx-&gt;log(MODX_LOG_LEVEL_ERROR, print_r($resource-&gt;toArray(),true) );
// Get the page id
$page_id = $resource-&gt;get(&#39;id&#39;);
// or simply
$page_id = $id;

if ($mode == &#39;new&#39;) {
    // resource created
}
else {
   // existing resource was updated
}
{code}

Anything you return from this event will be written to the logs, e.g.

{code}
return &quot;Help I&#39;m a bug!&quot;;
{code}

Will result in a log message like the following:

{code}
 [2012-06-22 13:00:28] (ERROR @ /connectors/resource/index.php) [OnDocFormSave]Help I&#39;m a bug!
{code}

h2. See Also

* [System Events]
* [Plugins]";}i:1;a:3:{s:2:"id";s:8:"39355855";s:3:"ver";s:1:"4";s:4:"code";s:937:"\\

h2. Event: OnDocFormSave

Fires after a Resource is saved in the manager via the editing form.

Service: 1 - Parser Service Events
Group: Documents

h2. Event Parameters

||Name||Description||
|mode|Either &#39;new&#39; or &#39;upd&#39;, depending on the circumstances.|
|resource|A reference to the modResource object.|
|id|The ID of the Resource (even for new resources)|

h2. Examples

To do something with the page ID (e.g. to update a related custom table), you can read this out of the *$resource* object (even if you are creating a new resource):

{code}
// Log all available properties of the $resource
$modx-&gt;log(MODX_LOG_LEVEL_ERROR, print_r($resource-&gt;toArray(),true) );
// Get the page id
$page_id = $resource-&gt;get(&#39;id&#39;);
// or simply
$page_id = $id;

if ($mode == &#39;new&#39;) {
    // resource created
}
else {
   // existing resource was updated
}
{code}

h2. See Also

* [System Events]
* [Plugins]";}i:2;a:3:{s:2:"id";s:8:"39355853";s:3:"ver";s:1:"3";s:4:"code";s:835:"\\

h2. Event: OnDocFormSave

Fires after a Resource is saved in the manager via the editing form.

Service: 1 - Parser Service Events
Group: Documents

h2. Event Parameters

||Name||Description||
|mode|Either &#39;new&#39; or &#39;upd&#39;, depending on the circumstances.|
|resource|A reference to the modResource object.|
|id|The ID of the Resource (even for new resources)|

h2. Examples

To do something with the page ID (e.g. to update a related custom table), you can read this out of the *$resource* object (even if you are creating a new resource):

{code}
// Log all available properties of the $resource
$modx-&gt;log(MODX_LOG_LEVEL_ERROR, print_r($resource-&gt;toArray(),true) );
// Get the page id
$page_id = $resource-&gt;get(&#39;id&#39;);
// or simply
$page_id = $id;
{code}

h2. See Also

* [System Events]
* [Plugins]";}i:3;a:3:{s:2:"id";s:8:"39355851";s:3:"ver";s:1:"2";s:4:"code";s:696:"\\

h2. Event: OnDocFormSave

Fires after a Resource is saved in the manager via the editing form.

Service: 1 - Parser Service Events
Group: Documents

h2. Event Parameters

||Name||Description||
|mode|Either &#39;new&#39; or &#39;upd&#39;, depending on the circumstances.|
|resource|A reference to the modResource object.|
|id|The ID of the Resource. Will be 0 for new Resources.|

h2. Examples

To do something with the page ID (e.g. to update a related custom table), you can read this out of the *$resource* object (even if you are creating a new resource):

{code}
$page_id = $resource-&gt;get(&#39;id&#39;);
// do something with $page_id
{code}

h2. See Also

* [System Events]
* [Plugins]";}i:4;a:3:{s:2:"id";s:8:"39355849";s:3:"ver";s:1:"1";s:4:"code";s:427:"\\

h2. Event: OnDocFormSave

Fires after a Resource is saved in the manager via the editing form.

Service: 1 - Parser Service Events
Group: Documents

h2. Event Parameters

||Name||Description||
|mode|Either &#39;new&#39; or &#39;upd&#39;, depending on the circumstances.|
|resource|A reference to the modResource object.|
|id|The ID of the Resource. Will be 0 for new Resources.|

h2. See Also

* [System Events]
* [Plugins]";}}}